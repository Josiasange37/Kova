<?xml version="1.0" encoding="utf-8"?>
<!-- Accessibility service config: metadata-only mode.
     We listen for window changes and notifications,
     but do NOT attempt to read message content.
     FLAG_SECURE blocks it on Android 12+. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeNotificationStateChanged"
    android:accessibilityFlags="flagDefault|flagReportViewIds"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="300"
    android:canRetrieveWindowContent="false"
    android:settingsActivity="com.kova.child.MainActivity" />
