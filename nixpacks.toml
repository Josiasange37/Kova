[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
    "cd server && npm cache clean --force",
    "cd server && npm install --omit=dev"
]

[phases.build]
cmds = []

[start]
cmd = "cd server && npm start"
